{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "watch-ts",
            "group": {
                "kind": "build",
            },
            "type": "shell",
            "command": "ab watch-ts",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$tsc"
            ]
        }
    ]
}